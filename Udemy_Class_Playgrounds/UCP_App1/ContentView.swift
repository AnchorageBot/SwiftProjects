import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "mountain.2")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Welcome!")
        }
    }
}
